{% macro alert(message, type) %}
    <div class="alert alert-dismissible alert-{{ type }}">
        <button type="button" class="btn-close" data-bs-dismiss="{{ type }}"></button>
        <p class="mb-0">{{ message }}</p>
    </div>
{% endmacro%}